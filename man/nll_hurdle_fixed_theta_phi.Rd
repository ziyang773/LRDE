% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hurdleNBTest.R
\name{nll_hurdle_fixed_theta_phi}
\alias{nll_hurdle_fixed_theta_phi}
\title{Negative Log-Likelihood for Hurdle NB with Fixed Zero Probabilities and Dispersion}
\usage{
nll_hurdle_fixed_theta_phi(
  params,
  counts,
  groups,
  offset = NULL,
  fixed_theta_A,
  fixed_theta_B,
  fixed_phi,
  one_group = TRUE
)
}
\arguments{
\item{params}{Numeric vector:
\describe{
\item{1}{If \code{one_group = TRUE}, log(mu) shared across groups.}
\item{1}{If \code{one_group = FALSE}, log(mu_A) for group A.}
\item{2}{If \code{one_group = FALSE}, delta_rate = log(mu_B) - log(mu_A).}
}}

\item{counts}{Numeric vector of observed counts for a single gene.}

\item{groups}{Factor indicating group membership for each observation.}

\item{offset}{Optional numeric vector of normalization factors. If \code{NULL},
defaults to 1 for all observations.}

\item{fixed_theta_A}{Numeric, fixed non-zero probability for group A.}

\item{fixed_theta_B}{Numeric, fixed non-zero probability for group B.}

\item{fixed_phi}{Numeric, fixed NB dispersion parameter (size).}

\item{one_group}{Logical; if \code{TRUE}, a single mean is shared across both groups.}
}
\value{
Numeric, the negative log-likelihood value.
}
\description{
Internal function to compute the negative log-likelihood of a hurdle
negative binomial model where both zero probabilities (\code{theta}) and
dispersion (\code{phi}) are fixed. Only mean parameters are optimized.
}
\details{
The likelihood consists of:
\itemize{
\item Bernoulli component for zero vs non-zero counts using fixed theta
\item Truncated negative binomial component for positive counts using fixed phi
}

This function is used internally during optimization when only mean parameters
are estimated, while theta and phi are kept constant.
}
\keyword{internal}
