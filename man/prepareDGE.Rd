% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimateSizeFactors.R
\name{prepareDGE}
\alias{prepareDGE}
\title{Prepare Count Data for Differential Expression Analysis}
\usage{
prepareDGE(data, group)
}
\arguments{
\item{data}{A numeric matrix, data.frame, or supported object containing counts.}

\item{group}{A vector of group labels for the columns/samples of \code{data}. Must be the same length as the number of columns in \code{data}.}
}
\value{
A list with two elements:
\describe{
\item{counts}{An integer matrix of counts.}
\item{samples}{A data.frame containing sample-level metadata: \code{group}, \code{lib.size}, and \code{size.factor}.}
}
}
\description{
Converts various supported input types to a standardized list format for downstream
differential expression analysis. Supports \code{matrix}, \code{data.frame}, \code{DGEList},
\code{DESeqDataSet}, and \code{SummarizedExperiment} objects.
}
\details{
This function performs input validation.
\itemize{
\item Checks for non-negative numeric values and absence of NA.
\item Ensures group labels match the number of samples.
\item Automatically assigns column names if missing.
\item Returns a list suitable for use with hurdle model-based DE functions.
}
}
\examples{
# Example with a matrix
set.seed(123)
mat <- matrix(rnbinom(30, size = 5, mu = 5), nrow = 5)
grp <- c("A", "A", "A", "B", "B", "B")
y <- prepareDGE(mat, grp)

# Example with a SummarizedExperiment
if (requireNamespace("SummarizedExperiment", quietly = TRUE)) {
    se <- SummarizedExperiment::SummarizedExperiment(assays = list(counts = mat))
    y_se <- prepareDGE(se, grp)
    y
}

}
